
module.exports = {
  theme: {
    // here you can extend the tailwind theme
    // the ones defined here have preference over the original config, so in case of using same names yours will win
  }
}
