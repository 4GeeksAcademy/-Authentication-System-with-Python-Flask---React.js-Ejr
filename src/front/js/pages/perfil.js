import React from "react";
import UploadImage from "../component/perfilInfo";

export const Home = () => {
	const { store, actions } = useContext(Context);

    return (
        <UploadImage />
    )}